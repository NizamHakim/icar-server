{
	"builds": [
		{
			"src": "package.json",
			"use": "@railway/node",
			"config": {
				"buildCommand": "npm install && npm run build",
				"startCommand": "npm run prisma:deploy && npm start"
			}
		}
	]
}
